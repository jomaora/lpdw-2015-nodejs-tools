var should = require('chai').should();     // calling function

describe('Geometry library', () => {
    describe('#triangle area', () => {
        it('should return 10 when base is 5 and height is 4');
    });
});